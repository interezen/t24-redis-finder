#################################################
# REDIS CONFIGURATION
#################################################
redis:
  - id: "redis-finder"
    use: true
    type: "single"
    uri: ["192.168.124.238:1332"]
    timeout: 1000
    command_timeout: 500
    topology_interval: 60000
    half_mode: "sync"
    auth: ""
    workers: 0
